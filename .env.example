# =============================================================================
# PSIQUIAPP - CONFIGURACIÓN DE VARIABLES DE ENTORNO
# =============================================================================
# Copia este archivo como .env y configura tus valores específicos
# NUNCA subas el archivo .env a control de versiones por seguridad

# =============================================================================
# CONFIGURACIÓN DEL SERVIDOR
# =============================================================================
# Puerto donde se ejecutará la aplicación
PORT=3000

# Entorno de ejecución (development, production)
NODE_ENV=development

# =============================================================================
# CONFIGURACIÓN DE BASE DE DATOS MYSQL
# =============================================================================
# REQUERIDO: Configuración de conexión a MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=tu_contraseña_mysql_aqui
DB_NAME=psiquiapp_db

# =============================================================================
# SEGURIDAD DE SESIONES
# =============================================================================
# REQUERIDO: Clave secreta para cifrar sesiones (genera una clave única y fuerte)
# Usa: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=tu_clave_secreta_muy_fuerte_aqui

# =============================================================================
# CONFIGURACIÓN HIPAA (PRODUCCIÓN)
# =============================================================================
# Configuraciones adicionales para cumplimiento HIPAA en producción

# Configuración SSL/TLS (cuando uses certificados)
# SSL_KEY_PATH=/path/to/private/key.pem
# SSL_CERT_PATH=/path/to/certificate/cert.pem

# Configuración de logging de auditoría
# AUDIT_LOG_LEVEL=info
# AUDIT_LOG_FILE=/var/log/psiquiapp/audit.log

# =============================================================================
# CONFIGURACIÓN DE EMAIL (OPCIONAL)
# =============================================================================
# Para notificaciones y recuperación de contraseñas
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=tu_email@ejemplo.com
# EMAIL_PASSWORD=tu_contraseña_email
# EMAIL_FROM=noreply@psiquiapp.com

# =============================================================================
# CONFIGURACIÓN DE BACKUP (OPCIONAL)
# =============================================================================
# Para respaldos automáticos de la base de datos
# BACKUP_SCHEDULE=0 2 * * *  # Diario a las 2 AM
# BACKUP_RETENTION_DAYS=30
# BACKUP_PATH=/var/backups/psiquiapp

# =============================================================================
# CONFIGURACIÓN DE DESARROLLO
# =============================================================================
# Solo para entorno de desarrollo
DEBUG=false
VERBOSE_LOGGING=false

# =============================================================================
# CONFIGURACIÓN DE SEGURIDAD ADICIONAL
# =============================================================================
# Rate limiting para APIs
RATE_LIMIT_WINDOW_MS=900000  # 15 minutos
RATE_LIMIT_MAX_REQUESTS=100

# Configuración CORS (si necesitas acceso desde otros dominios)
# CORS_ORIGIN=https://tudominio.com

# =============================================================================
# INSTRUCCIONES DE CONFIGURACIÓN
# =============================================================================
# 1. Copia este archivo como .env: cp .env.example .env
# 2. Configura las variables REQUERIDAS (DB_PASSWORD, SESSION_SECRET)
# 3. Genera una SESSION_SECRET segura usando:
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 4. Configura tu contraseña de MySQL en DB_PASSWORD
# 5. Asegúrate de que la base de datos psiquiapp_db exista
# 6. Ejecuta: npm start o pnpm start

# =============================================================================
# SEGURIDAD IMPORTANTE
# =============================================================================
# - NUNCA subas el archivo .env a GitHub/control de versiones
# - Usa contraseñas fuertes para DB_PASSWORD
# - Usa una SESSION_SECRET única y aleatoria
# - En producción, configura NODE_ENV=production
# - En producción, asegúrate de usar HTTPS
# - Realiza backups regulares de la base de datos